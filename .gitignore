# Secrets — never commit
.env
# Twitch user token + cached listings (sensitive)
render/.token.json
render/cli/.last-clips.json
# Downloaded clips / rendered reels
render/realclips/
# Saved reel configurations (local user data)
render/configs/
# Local clip catalog (rebuildable from Twitch via the Update button)
render/catalog.json

# Node
node_modules/

# Playwright
.playwright-mcp/

# Logs / temp
*.log
