# Secrets — never commit
.env
# Twitch user token + cached listings (sensitive)
render/.token.json
render/.last-clips.json
# Downloaded clips / rendered reels
render/realclips/
# Generated preview playlist (written by the server)
overlay/reel.playlist.json

# Node
node_modules/

# Logs / temp
*.log
